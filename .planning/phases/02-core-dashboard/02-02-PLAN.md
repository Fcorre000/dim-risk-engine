---
phase: 02-core-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/MainLayout.tsx
  - frontend/src/pages/OverviewPage.tsx
  - frontend/src/pages/PlaceholderPage.tsx
  - frontend/src/types/api.ts
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Dark-mode layout renders with sidebar on left and main content on right at lg breakpoint"
    - "Sidebar shows six nav links: Overview, Anomalies, By Zone, By SKU, Trends, Export"
    - "Clicking a sidebar link highlights the active item and renders the correct page in the content area"
    - "On mobile (<lg), layout collapses to single column (sidebar hidden by default)"
    - "TypeScript types for ShipmentResult exported from src/types/api.ts"
  artifacts:
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Left sidebar with six navigation links"
      contains: "Overview"
    - path: "frontend/src/components/layout/MainLayout.tsx"
      provides: "Responsive two-column layout wrapper"
    - path: "frontend/src/types/api.ts"
      provides: "ShipmentResult TypeScript interface matching backend Pydantic model"
      contains: "ShipmentResult"
    - path: "frontend/src/App.tsx"
      provides: "Root app with page routing via useState"
      contains: "MainLayout"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/layout/MainLayout.tsx"
      via: "import and render"
      pattern: "import.*MainLayout"
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "active page state"
      via: "onNavigate prop"
      pattern: "onNavigate"
---

<objective>
Build the dark-mode layout shell: sidebar navigation with six links, responsive two-column layout, page routing via React useState, and TypeScript type definitions for the API response.

Purpose: Every downstream plan (03-05) builds components that slot into this layout. The API types defined here are the contract all data-consuming components import.
Output: A navigable dark-mode shell. All six sidebar links render placeholder pages. MainLayout handles responsive lg:flex split.
</objective>

<execution_context>
@/Users/fortnitekorea/Desktop/dim-risk-engine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fortnitekorea/Desktop/dim-risk-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-dashboard/02-01-SUMMARY.md

<interfaces>
<!-- From Phase 1 api/main.py — ShipmentResult Pydantic model -->
class ShipmentResult(BaseModel):
    tracking_number: str
    dim_flag_probability: float    # 0.0 to 1.0
    predicted_net_charge: float    # dollars after np.expm1()
    dim_anomaly: Optional[str]     # "Unexpected" | null
    cost_anomaly: Optional[str]    # "Review" | null

<!-- Tailwind color tokens established in 02-01 -->
bg-gray-950  → page background
bg-gray-900  → card / sidebar background
bg-gray-800  → table rows / inputs
bg-gray-700  → hover states
blue-500     → active nav item / primary action
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define API types and create Sidebar + MainLayout components</name>
  <read_first>
    - /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/src/App.tsx
    - /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/tailwind.config.js
    - /Users/fortnitekorea/Desktop/dim-risk-engine/.claude/skills/ui-ux-pro-max/.claude/skills/ui-ux-pro-max/SKILL.md (lines 52-130, priority rules 1, 4, 5, 9)
  </read_first>
  <files>frontend/src/types/api.ts, frontend/src/components/layout/Sidebar.tsx, frontend/src/components/layout/MainLayout.tsx</files>
  <action>
Create three files. Create directories frontend/src/types/ and frontend/src/components/layout/ first.

**frontend/src/types/api.ts** — TypeScript mirror of ShipmentResult Pydantic model:
```typescript
export interface ShipmentResult {
  tracking_number: string;
  dim_flag_probability: number;   // 0.0 to 1.0
  predicted_net_charge: number;   // dollars
  dim_anomaly: 'Unexpected' | null;
  cost_anomaly: 'Review' | null;
}

export type PageId = 'overview' | 'anomalies' | 'by-zone' | 'by-sku' | 'trends' | 'export';

export interface UploadState {
  status: 'idle' | 'uploading' | 'complete' | 'error';
  filename: string | null;
  shipmentCount: number | null;
  analysisTimeMs: number | null;
  results: ShipmentResult[] | null;
  errorMessage: string | null;
}
```

**frontend/src/components/layout/Sidebar.tsx**:

Design per SKILL.md:
- Priority 9 (Navigation): Predictable, highlighted active item, ≤6 links
- Priority 4 (Style): Dark mode, consistent SVG icons (Heroicons inline), no emoji
- Priority 1 (Accessibility): aria-label on nav, aria-current="page" on active link
- Priority 2 (Touch): Min 44px touch targets on nav items
- Priority 5 (Responsive): Hidden on mobile, full on lg+

```typescript
import type { PageId } from '../../types/api';

interface NavItem {
  id: PageId;
  label: string;
  icon: React.ReactNode;
}

const NAV_ITEMS: NavItem[] = [
  {
    id: 'overview',
    label: 'Overview',
    icon: (
      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5} aria-hidden="true">
        <path strokeLinecap="round" strokeLinejoin="round" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
      </svg>
    ),
  },
  {
    id: 'anomalies',
    label: 'Anomalies',
    icon: (
      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5} aria-hidden="true">
        <path strokeLinecap="round" strokeLinejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
      </svg>
    ),
  },
  {
    id: 'by-zone',
    label: 'By Zone',
    icon: (
      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5} aria-hidden="true">
        <path strokeLinecap="round" strokeLinejoin="round" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7" />
      </svg>
    ),
  },
  {
    id: 'by-sku',
    label: 'By SKU',
    icon: (
      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5} aria-hidden="true">
        <path strokeLinecap="round" strokeLinejoin="round" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
      </svg>
    ),
  },
  {
    id: 'trends',
    label: 'Trends',
    icon: (
      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5} aria-hidden="true">
        <path strokeLinecap="round" strokeLinejoin="round" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
      </svg>
    ),
  },
  {
    id: 'export',
    label: 'Export',
    icon: (
      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5} aria-hidden="true">
        <path strokeLinecap="round" strokeLinejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
      </svg>
    ),
  },
];

interface SidebarProps {
  activePage: PageId;
  onNavigate: (page: PageId) => void;
}

export default function Sidebar({ activePage, onNavigate }: SidebarProps) {
  return (
    <nav
      aria-label="Main navigation"
      className="flex flex-col w-56 min-h-screen bg-gray-900 border-r border-gray-800 px-3 py-6 flex-shrink-0"
    >
      {/* Logo / App title */}
      <div className="flex items-center gap-2 px-3 mb-8">
        <div className="w-7 h-7 rounded bg-blue-600 flex items-center justify-center flex-shrink-0">
          <span className="text-white text-xs font-bold">DR</span>
        </div>
        <span className="text-white font-semibold text-sm tracking-wide">DimRisk</span>
      </div>

      {/* Nav items */}
      <ul className="space-y-1 flex-1">
        {NAV_ITEMS.map((item) => {
          const isActive = activePage === item.id;
          return (
            <li key={item.id}>
              <button
                onClick={() => onNavigate(item.id)}
                aria-current={isActive ? 'page' : undefined}
                className={[
                  'w-full flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium transition-colors duration-150 cursor-pointer',
                  'min-h-[44px]',  // SKILL.md: touch target minimum 44px
                  isActive
                    ? 'bg-blue-600 text-white'
                    : 'text-gray-400 hover:bg-gray-800 hover:text-gray-100',
                ].join(' ')}
              >
                {item.icon}
                {item.label}
              </button>
            </li>
          );
        })}
      </ul>

      {/* Footer version */}
      <div className="px-3 mt-4">
        <p className="text-xs text-gray-600">v1.0 · DimRisk Engine</p>
      </div>
    </nav>
  );
}
```

**frontend/src/components/layout/MainLayout.tsx**:
```typescript
import type { ReactNode } from 'react';
import type { PageId } from '../../types/api';
import Sidebar from './Sidebar';

interface MainLayoutProps {
  activePage: PageId;
  onNavigate: (page: PageId) => void;
  children: ReactNode;
}

export default function MainLayout({ activePage, onNavigate, children }: MainLayoutProps) {
  return (
    // SKILL.md Priority 5 (Responsive): lg:flex for two-column, mobile stacks
    <div className="flex min-h-screen bg-gray-950">
      {/* Sidebar hidden on mobile, shown on lg+ */}
      <div className="hidden lg:flex">
        <Sidebar activePage={activePage} onNavigate={onNavigate} />
      </div>

      {/* Main content area */}
      <main className="flex-1 overflow-y-auto">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
          {children}
        </div>
      </main>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>grep -n "ShipmentResult" /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/src/types/api.ts && grep -n "aria-current" /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/src/components/layout/Sidebar.tsx && grep -n "hidden lg:flex" /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/src/components/layout/MainLayout.tsx</automated>
  </verify>
  <done>api.ts exports ShipmentResult, PageId, UploadState interfaces. Sidebar.tsx renders 6 nav links with aria-current on active. MainLayout.tsx has `hidden lg:flex` responsive wrapper for sidebar.</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx with MainLayout, page router, and placeholder pages</name>
  <read_first>
    - /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/src/App.tsx
    - /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/src/components/layout/MainLayout.tsx
    - /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/src/types/api.ts
  </read_first>
  <files>frontend/src/App.tsx, frontend/src/pages/OverviewPage.tsx, frontend/src/pages/PlaceholderPage.tsx</files>
  <action>
Replace the placeholder App.tsx with one that wires MainLayout + page routing. Create pages directory and two page files.

**frontend/src/pages/PlaceholderPage.tsx** — used for all non-Overview pages in Phase 2:
```typescript
interface PlaceholderPageProps {
  title: string;
}

export default function PlaceholderPage({ title }: PlaceholderPageProps) {
  return (
    <div className="flex items-center justify-center min-h-64">
      <div className="text-center">
        <h1 className="text-2xl font-semibold text-gray-100 mb-2">{title}</h1>
        <p className="text-gray-500 text-sm">Coming in Phase 3+</p>
      </div>
    </div>
  );
}
```

**frontend/src/pages/OverviewPage.tsx** — shell only; 02-03, 02-04, 02-05 will add content:
```typescript
import type { UploadState } from '../types/api';

interface OverviewPageProps {
  uploadState: UploadState;
  onUpload: (file: File) => Promise<void>;
}

export default function OverviewPage({ uploadState, onUpload }: OverviewPageProps) {
  return (
    <div className="space-y-8">
      <div>
        <h1 className="text-2xl font-semibold text-gray-100">Overview</h1>
        <p className="text-gray-500 text-sm mt-1">Upload an invoice to analyze DIM billing anomalies</p>
      </div>
      {/* Upload component slot — added in 02-03 */}
      {/* KPI cards slot — added in 02-04 */}
      {/* Charts slot — added in 02-04, 02-05 */}
      {/* Anomaly table slot — added in 02-05 */}
    </div>
  );
}
```

**frontend/src/App.tsx** — replace fully:
```typescript
import { useState } from 'react';
import type { PageId, UploadState } from './types/api';
import MainLayout from './components/layout/MainLayout';
import OverviewPage from './pages/OverviewPage';
import PlaceholderPage from './pages/PlaceholderPage';

const INITIAL_UPLOAD_STATE: UploadState = {
  status: 'idle',
  filename: null,
  shipmentCount: null,
  analysisTimeMs: null,
  results: null,
  errorMessage: null,
};

export default function App() {
  const [activePage, setActivePage] = useState<PageId>('overview');
  const [uploadState, setUploadState] = useState<UploadState>(INITIAL_UPLOAD_STATE);

  // onUpload will be implemented in 02-03 with real API call
  const handleUpload = async (_file: File): Promise<void> => {
    // Placeholder — replaced in 02-03
    setUploadState((prev) => ({ ...prev, status: 'uploading' }));
  };

  const renderPage = () => {
    switch (activePage) {
      case 'overview':
        return <OverviewPage uploadState={uploadState} onUpload={handleUpload} />;
      case 'anomalies':
        return <PlaceholderPage title="Anomalies" />;
      case 'by-zone':
        return <PlaceholderPage title="By Zone" />;
      case 'by-sku':
        return <PlaceholderPage title="By SKU" />;
      case 'trends':
        return <PlaceholderPage title="Trends" />;
      case 'export':
        return <PlaceholderPage title="Export" />;
      default:
        return <PlaceholderPage title="Page Not Found" />;
    }
  };

  return (
    <MainLayout activePage={activePage} onNavigate={setActivePage}>
      {renderPage()}
    </MainLayout>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/fortnitekorea/Desktop/dim-risk-engine/frontend && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MainLayout" frontend/src/App.tsx` returns a match
    - `grep -n "PageId" frontend/src/App.tsx` returns a match
    - `grep -n "UploadState" frontend/src/App.tsx` returns a match
    - `npm run build` exits with code 0 (TypeScript compiles without errors)
    - `grep -n "aria-current" frontend/src/components/layout/Sidebar.tsx` returns a match
    - `grep -n "hidden lg:flex" frontend/src/components/layout/MainLayout.tsx` returns a match
    - `grep -n "ShipmentResult" frontend/src/types/api.ts` returns a match
  </acceptance_criteria>
  <done>App.tsx renders MainLayout wrapping page router. TypeScript build passes. Sidebar has 6 nav items with accessibility attributes. ShipmentResult, PageId, UploadState exported from types/api.ts.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/fortnitekorea/Desktop/dim-risk-engine/frontend && npm run build 2>&1 | tail -5
grep -n "ShipmentResult" frontend/src/types/api.ts
grep -rn "aria-current" frontend/src/components/layout/Sidebar.tsx
grep -n "hidden lg:flex" frontend/src/components/layout/MainLayout.tsx
grep -n "MainLayout" frontend/src/App.tsx
```
All five commands must produce matches. Build must exit 0.
</verification>

<success_criteria>
- frontend/src/types/api.ts exports ShipmentResult, PageId, UploadState
- frontend/src/components/layout/Sidebar.tsx renders 6 nav links with aria-current on active item
- frontend/src/components/layout/MainLayout.tsx uses `hidden lg:flex` for responsive sidebar
- frontend/src/App.tsx wires MainLayout with useState-based page router
- `npm run build` exits with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-dashboard/02-02-SUMMARY.md`
</output>
