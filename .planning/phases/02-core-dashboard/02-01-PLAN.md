---
phase: 02-core-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/index.css
  - frontend/src/App.tsx
autonomous: true
requirements:
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Running `npm run dev` inside frontend/ starts Vite dev server on port 5173 without errors"
    - "Tailwind CSS utility classes render correctly (dark: prefix works)"
    - "Recharts is importable without runtime errors"
    - "TypeScript compiles without errors (`npm run build` passes)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Project dependencies including react, vite, tailwindcss, recharts, typescript"
      contains: "recharts"
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind config with dark mode class strategy"
      contains: "darkMode"
    - path: "frontend/src/index.css"
      provides: "Tailwind directives"
      contains: "@tailwind base"
    - path: "frontend/src/App.tsx"
      provides: "Root component placeholder"
  key_links:
    - from: "frontend/src/index.css"
      to: "Tailwind CSS utilities"
      via: "@tailwind directives"
      pattern: "@tailwind base"
    - from: "frontend/tailwind.config.js"
      to: "src files"
      via: "content glob"
      pattern: "src/\\*\\*/\\*.\\{ts,tsx\\}"
---

<objective>
Scaffold the frontend/ React + Vite + TypeScript project with Tailwind CSS and Recharts installed, dev server running, and dark mode enabled.

Purpose: Every subsequent plan builds React components on this foundation. Vite + Tailwind + Recharts must be confirmed working before any UI work begins.
Output: A runnable frontend/ directory. `npm run dev` starts without errors. `npm run build` compiles without TypeScript errors. Tailwind dark: prefix functional. Recharts importable.
</objective>

<execution_context>
@/Users/fortnitekorea/Desktop/dim-risk-engine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fortnitekorea/Desktop/dim-risk-engine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- API contract from Phase 1 (api/main.py) -->
POST /analyze
  - Accepts: multipart/form-data with `file` field (.xlsx or .csv)
  - Returns: JSON array of ShipmentResult objects

ShipmentResult shape:
  tracking_number: string
  dim_flag_probability: number   // 0.0-1.0
  predicted_net_charge: number   // dollars
  dim_anomaly: "Unexpected" | null
  cost_anomaly: "Review" | null

Backend runs at http://localhost:8000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold React + Vite + TypeScript project in frontend/</name>
  <read_first>
    - /Users/fortnitekorea/Desktop/dim-risk-engine/.claude/skills/ui-ux-pro-max/.claude/skills/ui-ux-pro-max/SKILL.md (lines 1-130)
    - Check if frontend/ already exists: `ls /Users/fortnitekorea/Desktop/dim-risk-engine/frontend 2>/dev/null`
  </read_first>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/vite-env.d.ts</files>
  <action>
Create the frontend/ directory scaffold for a React 18 + Vite 5 + TypeScript project. Do NOT use create-react-app. Create files manually.

**frontend/package.json** — exact content:
```json
{
  "name": "dim-risk-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.5.3",
    "vite": "^5.3.4"
  }
}
```

**frontend/vite.config.ts**:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
  },
})
```

**frontend/tsconfig.json**:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
```

**frontend/index.html**:
```html
<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DimRisk Engine</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```
Note: `class="dark"` on `<html>` enables Tailwind dark mode globally.

**frontend/src/main.tsx**:
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

**frontend/src/App.tsx** — placeholder only (02-02 will replace):
```typescript
function App() {
  return (
    <div className="min-h-screen bg-gray-950 text-gray-100">
      <p className="p-8 text-lg">DimRisk Engine — loading...</p>
    </div>
  )
}

export default App
```

**frontend/src/vite-env.d.ts**:
```typescript
/// <reference types="vite/client" />
```

After creating all files, run from the frontend/ directory:
```bash
cd /Users/fortnitekorea/Desktop/dim-risk-engine/frontend && npm install
```
  </action>
  <verify>
    <automated>cd /Users/fortnitekorea/Desktop/dim-risk-engine/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>frontend/package.json exists with recharts and tailwindcss listed. `npm run build` completes without TypeScript errors. node_modules/ contains recharts/ and tailwindcss/.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS with dark mode class strategy</name>
  <read_first>
    - /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/package.json (just created)
    - /Users/fortnitekorea/Desktop/dim-risk-engine/.claude/skills/ui-ux-pro-max/.claude/skills/ui-ux-pro-max/SKILL.md (lines 52-63, priority 6 — Typography & Color)
  </read_first>
  <files>frontend/tailwind.config.js, frontend/postcss.config.js, frontend/src/index.css</files>
  <action>
Create three files to complete Tailwind configuration.

**frontend/tailwind.config.js**:
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{ts,tsx}",
  ],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        // DimRisk brand tokens — use these in all components
        // Background hierarchy (dark mode):
        // bg-gray-950  → page background
        // bg-gray-900  → card / sidebar background
        // bg-gray-800  → table row / input background
        // bg-gray-700  → hover states
        //
        // Accent: blue-500 (primary action), blue-400 (hover)
        // Success: emerald-500
        // Warning: amber-400
        // Danger/flag: rose-500
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
```

**frontend/postcss.config.js**:
```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

**frontend/src/index.css** — Tailwind directives + Inter font import + base styles:
```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-gray-950 text-gray-100 font-sans antialiased;
    font-size: 16px;
    line-height: 1.5;
  }

  /* Scrollbar styling for dark mode */
  ::-webkit-scrollbar {
    width: 6px;
    height: 6px;
  }
  ::-webkit-scrollbar-track {
    @apply bg-gray-900;
  }
  ::-webkit-scrollbar-thumb {
    @apply bg-gray-700 rounded;
  }

  /* Focus ring per SKILL.md accessibility rule */
  *:focus-visible {
    @apply outline-none ring-2 ring-blue-500 ring-offset-2 ring-offset-gray-950;
  }
}
```

This establishes the color system per SKILL.md Priority 6 (Typography & Color):
- Base font size 16px, line-height 1.5
- Semantic tokens: gray-950/900/800 for backgrounds, blue-500 for accent
- Focus rings per Priority 1 (Accessibility)
  </action>
  <verify>
    <automated>cd /Users/fortnitekorea/Desktop/dim-risk-engine/frontend && grep -n "darkMode" tailwind.config.js && grep -n "@tailwind base" src/index.css && npm run build 2>&1 | grep -E "error|built in|✓" | head -5</automated>
  </verify>
  <done>tailwind.config.js has `darkMode: 'class'` and content glob `./src/**/*.{ts,tsx}`. index.css has all three @tailwind directives. `npm run build` succeeds with no errors. Built output in frontend/dist/ includes CSS with Tailwind utilities.</done>
</task>

</tasks>

<verification>
Run from project root:
```bash
cd /Users/fortnitekorea/Desktop/dim-risk-engine/frontend && npm run build
```
Expected: TypeScript compiles, Vite bundles, `dist/` directory created.

```bash
grep "recharts" /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/package.json
grep "darkMode" /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/tailwind.config.js
grep "@tailwind base" /Users/fortnitekorea/Desktop/dim-risk-engine/frontend/src/index.css
```
All three greps must return matches.
</verification>

<success_criteria>
- frontend/package.json contains "recharts", "tailwindcss", "vite", "@vitejs/plugin-react"
- frontend/tailwind.config.js has `darkMode: 'class'` and content includes `./src/**/*.{ts,tsx}`
- frontend/src/index.css has @tailwind base, @tailwind components, @tailwind utilities
- `npm run build` exits with code 0
- frontend/dist/ directory exists after build
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-dashboard/02-01-SUMMARY.md`
</output>
